import styled from 'styled-components';

const ButtonLogout = styled.button`
  border: none;
  background: transparent;
`;
const ButtonName = styled.button`
  border: none;
  background: transparent;
`;
const Li = styled.li`
  display: flex;
  flex-direction: row;
`;
export {ButtonName, Li};
export default ButtonLogout;
